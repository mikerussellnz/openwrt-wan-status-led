# openwrt-wan-status-led
Sets WAN status LED according to WAN ping test

## Installation

Copy 'wan_status_led' script to /etc/init.d ensuring it has +x execute permission. 
Run 'service wan_status_led enable' and start it or enable it through 'System' -> 'Startup' and start it.

